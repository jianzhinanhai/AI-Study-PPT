<!DOCTYPE html>

<html lang="zh-CN">
<head>
<meta charset="utf-8"/>
<meta content="width=device-width, initial-scale=1, viewport-fit=cover" name="viewport"/>
<meta content="#123d38" name="theme-color"/>
<title>南洋双城 · 家庭私享之旅｜吉隆坡 × 槟城</title>
<style>
:root {
  --ink:#152321;
  --deep:#103b36;
  --deep2:#0b2c29;
  --jade:#2d6b62;
  --sage:#dfe9e3;
  --cream:#f7f3ec;
  --paper:#fffdf9;
  --gold:#b98a46;
  --muted:#67726f;
  --line:#d9ded9;
  --shadow:0 18px 55px rgba(17,45,41,.12);
  --shadow-soft:0 10px 30px rgba(17,45,41,.08);
}
*{box-sizing:border-box}
html{scroll-behavior:smooth}
body{
  margin:0;background:var(--cream);color:var(--ink);
  font-family:"PingFang SC","Microsoft YaHei","Noto Sans CJK SC",Arial,sans-serif;
  line-height:1.65;-webkit-font-smoothing:antialiased;
}
a{color:inherit}
img{display:block;width:100%}
.wrap{max-width:1180px;margin:0 auto;padding:0 30px}
.topbar{
  position:fixed;z-index:50;left:0;right:0;top:0;
  background:rgba(8,32,29,.82);backdrop-filter:blur(16px);
  color:white;border-bottom:1px solid rgba(255,255,255,.12)
}
.topbar .inner{height:64px;display:flex;align-items:center;justify-content:space-between;gap:24px}
.brand{font-weight:700;letter-spacing:.14em;font-size:14px}
.nav{display:flex;gap:24px;font-size:13px;color:rgba(255,255,255,.78)}
.nav a{text-decoration:none}
.nav a:hover{color:#fff}
.print-btn{
  border:1px solid rgba(255,255,255,.35);color:#fff;background:transparent;
  border-radius:999px;padding:8px 14px;cursor:pointer;font-size:12px
}
.hero{
  min-height:790px;position:relative;display:flex;align-items:flex-end;
  background-image:linear-gradient(90deg,rgba(5,25,23,.88) 0%,rgba(5,25,23,.62) 47%,rgba(5,25,23,.22) 78%),
                   linear-gradient(0deg,rgba(5,25,23,.86) 0%,rgba(5,25,23,0) 55%),
                   url('__IMG_01__');
  background-size:cover;background-position:center 50%;
  color:white;padding:130px 0 78px;
}
.hero:after{content:"";position:absolute;inset:auto 0 0;height:7px;background:linear-gradient(90deg,var(--gold),#e0be7b,var(--jade))}
.hero-content{position:relative;z-index:2;max-width:820px}
.eyebrow{display:flex;align-items:center;gap:12px;font-size:12px;letter-spacing:.28em;text-transform:uppercase;color:#e8d2a7;font-weight:700}
.eyebrow:before{content:"";width:42px;height:1px;background:#d7b26b}
h1{font-family:"Songti SC","STSong","Noto Serif CJK SC",serif;font-size:72px;line-height:1.08;margin:22px 0 18px;letter-spacing:.02em;font-weight:700}
.hero-sub{font-size:22px;max-width:720px;color:rgba(255,255,255,.86);margin:0 0 28px}
.hero-meta{display:flex;flex-wrap:wrap;gap:10px}
.pill{padding:9px 15px;border:1px solid rgba(255,255,255,.28);border-radius:999px;background:rgba(255,255,255,.08);font-size:13px}
.hero-note{margin-top:28px;max-width:690px;padding-left:18px;border-left:2px solid #d8b674;color:rgba(255,255,255,.76);font-size:13px}
.section{padding:92px 0}
.section.white{background:var(--paper)}
.section.deep{background:var(--deep2);color:white}
.section-head{display:grid;grid-template-columns:1fr 1.15fr;gap:64px;align-items:end;margin-bottom:44px}
.kicker{font-size:12px;letter-spacing:.22em;color:var(--gold);font-weight:700;text-transform:uppercase;margin-bottom:10px}
h2{font-family:"Songti SC","STSong","Noto Serif CJK SC",serif;font-size:44px;line-height:1.18;margin:0}
.section-intro{color:var(--muted);font-size:16px;margin:0}
.deep .section-intro{color:rgba(255,255,255,.68)}
.stats{display:grid;grid-template-columns:repeat(4,1fr);gap:16px;margin-top:-58px;position:relative;z-index:5}
.stat{background:var(--paper);box-shadow:var(--shadow);padding:25px 22px;border-top:3px solid var(--gold)}
.stat b{display:block;font-family:"Songti SC","STSong",serif;font-size:28px;color:var(--deep)}
.stat span{font-size:12px;color:var(--muted);letter-spacing:.05em}
.highlights{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}
.highlight{padding:28px;background:white;border:1px solid var(--line);min-height:190px;box-shadow:var(--shadow-soft)}
.highlight .num{font-family:Georgia,serif;color:var(--gold);font-size:13px;letter-spacing:.14em}
.highlight h3{font-size:19px;margin:14px 0 8px}
.highlight p{font-size:14px;color:var(--muted);margin:0}
.route-card{background:white;box-shadow:var(--shadow);padding:38px 36px;border-radius:2px}
.route{display:grid;grid-template-columns:repeat(5,1fr);align-items:center;gap:0}
.city{text-align:center;position:relative}
.city .dot{width:12px;height:12px;border:3px solid white;background:var(--gold);box-shadow:0 0 0 1px var(--gold);border-radius:50%;margin:0 auto 12px;position:relative;z-index:2}
.city:not(:last-child):after{content:"";position:absolute;top:6px;left:50%;width:100%;height:1px;background:linear-gradient(90deg,var(--gold),#cbd6d0)}
.city b{display:block;font-size:16px}
.city small{color:var(--muted);font-size:11px}
.flight-table,.cost-table{width:100%;border-collapse:collapse;background:white;box-shadow:var(--shadow-soft)}
.flight-table th,.flight-table td,.cost-table th,.cost-table td{padding:16px 18px;text-align:left;border-bottom:1px solid #e8ece8;vertical-align:top}
.flight-table th,.cost-table th{font-size:11px;letter-spacing:.12em;color:var(--muted);background:#f2f5f2;text-transform:uppercase}
.flight-table td{font-size:14px}
.flight-table tr:last-child td,.cost-table tr:last-child td{border-bottom:0}
.tag{display:inline-block;padding:3px 9px;background:var(--sage);color:var(--deep);border-radius:999px;font-size:11px;font-weight:700;white-space:nowrap}
.warning{background:#fff8e8;border-left:3px solid var(--gold);padding:16px 18px;margin-top:18px;font-size:13px;color:#65573f}
.timeline{display:flex;flex-direction:column;gap:24px}
.day{
  display:grid;grid-template-columns:160px 1fr;background:white;
  box-shadow:var(--shadow-soft);min-height:260px
}
.day-side{background:var(--deep);color:white;padding:28px 22px;display:flex;flex-direction:column;justify-content:space-between}
.day-side .date{font-size:13px;letter-spacing:.12em;color:#e4c98f}
.day-side .dnum{font-family:Georgia,serif;font-size:46px;line-height:1}
.day-side .city-name{font-size:15px;font-weight:700}
.day-main{display:grid;grid-template-columns:1.3fr .9fr;gap:26px;padding:28px 30px}
.day-main h3{font-family:"Songti SC","STSong",serif;font-size:25px;margin:0 0 10px;color:var(--deep)}
.day-main p{margin:0 0 12px;color:var(--muted);font-size:14px}
.schedule{list-style:none;padding:0;margin:14px 0 0}
.schedule li{display:grid;grid-template-columns:72px 1fr;gap:12px;padding:8px 0;border-top:1px dashed #dde3de;font-size:13px}
.schedule time{font-weight:700;color:var(--jade)}
.day-photo{height:100%;min-height:210px;object-fit:cover}
.place-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:20px}
.place{background:white;box-shadow:var(--shadow-soft);overflow:hidden}
.place img{height:220px;object-fit:cover}
.place-copy{padding:22px 22px 24px}
.place-city{font-size:10px;letter-spacing:.18em;text-transform:uppercase;color:var(--gold);font-weight:700}
.place h3{font-family:"Songti SC","STSong",serif;font-size:22px;margin:7px 0 8px}
.place p{font-size:13px;color:var(--muted);margin:0}
.cost-overview{display:grid;grid-template-columns:1fr 1fr;gap:24px}
.cost-block{background:white;box-shadow:var(--shadow-soft)}
.cost-title{padding:24px 24px 15px;border-bottom:1px solid var(--line);display:flex;justify-content:space-between;align-items:flex-end}
.cost-title h3{margin:0;font-family:"Songti SC","STSong",serif;font-size:24px;color:var(--deep)}
.cost-title span{font-size:12px;color:var(--muted)}
.cost-table td:nth-child(2),.cost-table td:nth-child(3){text-align:right;white-space:nowrap}
.cost-table .subtotal td{background:#eff4f0;font-weight:700;color:var(--deep)}
.total-band{margin-top:26px;background:var(--deep);color:white;padding:30px;display:grid;grid-template-columns:1fr auto;gap:24px;align-items:center}
.total-band h3{margin:0;font-family:"Songti SC","STSong",serif;font-size:27px}
.total-band p{margin:4px 0 0;color:rgba(255,255,255,.65);font-size:13px}
.total-num{font-size:32px;font-weight:700;color:#e8cf9c;white-space:nowrap}
.assumptions{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin-top:18px}
.assumption{border:1px solid rgba(255,255,255,.14);padding:16px;font-size:12px;color:rgba(255,255,255,.67)}
.assumption b{display:block;color:white;font-size:13px;margin-bottom:4px}
.checklist{display:grid;grid-template-columns:repeat(2,1fr);gap:18px}
.check{background:white;border-top:3px solid var(--jade);padding:24px;box-shadow:var(--shadow-soft)}
.check h3{margin:0 0 8px;font-size:17px}
.check p{margin:0;color:var(--muted);font-size:13px}
.footer{background:#071f1c;color:rgba(255,255,255,.62);padding:54px 0 36px;font-size:11px}
.footer-grid{display:grid;grid-template-columns:1fr 1fr;gap:50px}
.footer h4{color:white;font-size:13px;margin:0 0 12px}
.footer p{margin:0 0 8px}
.credits{columns:2;column-gap:24px}
.credits div{break-inside:avoid;margin-bottom:6px}
.source-note{margin-top:28px;padding-top:20px;border-top:1px solid rgba(255,255,255,.12)}
.backtop{position:fixed;right:22px;bottom:22px;width:42px;height:42px;border-radius:50%;border:0;background:var(--deep);color:white;box-shadow:var(--shadow);cursor:pointer;z-index:40}
@media(max-width:900px){
  .nav{display:none} h1{font-size:52px} .hero{min-height:700px}
  .stats{grid-template-columns:repeat(2,1fr)} .section-head{grid-template-columns:1fr;gap:18px}
  .highlights,.place-grid{grid-template-columns:1fr 1fr}
  .day{grid-template-columns:110px 1fr} .day-main{grid-template-columns:1fr} .day-photo{height:260px}
  .cost-overview{grid-template-columns:1fr} .assumptions{grid-template-columns:1fr 1fr}
}
@media(max-width:620px){
  .wrap{padding:0 18px} .topbar .inner{height:56px}
  .hero{min-height:680px;padding:110px 0 62px;background-position:62% center}
  h1{font-size:42px} .hero-sub{font-size:17px}
  .section{padding:66px 0} h2{font-size:34px}
  .stats{margin-top:-34px;gap:10px} .stat{padding:18px 14px} .stat b{font-size:23px}
  .highlights,.place-grid,.checklist{grid-template-columns:1fr}
  .route-card{padding:26px 14px;overflow-x:auto} .route{min-width:620px}
  .flight-table,.cost-table{font-size:12px;display:block;overflow-x:auto;white-space:nowrap}
  .day{grid-template-columns:1fr} .day-side{min-height:115px;display:grid;grid-template-columns:auto 1fr auto;align-items:center;gap:15px}
  .day-side .dnum{font-size:34px} .day-main{padding:22px 20px} .schedule li{grid-template-columns:64px 1fr}
  .place img{height:230px} .total-band{grid-template-columns:1fr} .total-num{font-size:27px}
  .assumptions{grid-template-columns:1fr} .footer-grid{grid-template-columns:1fr} .credits{columns:1}
}
@media print{
  @page{size:A4;margin:12mm}
  body{background:white;font-size:10pt} .topbar,.backtop,.print-btn{display:none!important}
  .hero{min-height:230mm;page-break-after:always;padding-top:40mm}
  .section{padding:14mm 0} .stats{margin-top:0}
  .day,.place,.cost-block,.highlight,.check{break-inside:avoid;box-shadow:none}
  .section.deep{background:white;color:var(--ink)} .deep .section-intro{color:var(--muted)}
  .assumption{border-color:#bbb;color:var(--muted)} .assumption b{color:var(--ink)}
  .total-band{background:#eee;color:var(--ink)} .total-band p{color:var(--muted)} .total-num{color:var(--deep)}
  .footer{background:white;color:#555} .footer h4{color:var(--ink)} .footer .source-note{border-color:#ccc}
}

/* Interactive budget calculator */
.calc-shell{margin-top:34px;background:#fff;color:var(--ink);box-shadow:var(--shadow);border-top:4px solid var(--gold);overflow:hidden}
.calc-head{padding:28px 28px 20px;background:linear-gradient(135deg,#f8f4eb,#eef4ef);display:flex;justify-content:space-between;align-items:flex-end;gap:20px;border-bottom:1px solid var(--line)}
.calc-head h3{margin:0;font-family:"Songti SC","STSong",serif;font-size:28px;color:var(--deep)}
.calc-head p{margin:6px 0 0;color:var(--muted);font-size:13px}
.calc-tools{display:flex;gap:10px;flex-wrap:wrap;justify-content:flex-end}
.calc-btn{border:1px solid #bfc9c3;background:#fff;color:var(--deep);padding:9px 14px;border-radius:999px;cursor:pointer;font-size:12px;font-weight:700}
.calc-btn.primary{background:var(--deep);color:#fff;border-color:var(--deep)}
.calc-btn:hover{transform:translateY(-1px)}
.calc-scroll{overflow-x:auto}
.budget-editor{width:100%;border-collapse:collapse;min-width:940px}
.budget-editor th,.budget-editor td{padding:13px 12px;border-bottom:1px solid #e7ece8;vertical-align:middle}
.budget-editor thead th{background:#f3f6f3;color:var(--muted);font-size:11px;letter-spacing:.08em;text-transform:uppercase}
.budget-editor .group-row td{background:#eaf1ec;color:var(--deep);font-weight:700;font-size:14px;border-top:2px solid #d2ded6}
.budget-editor td:first-child{font-weight:700;color:var(--deep)}
.budget-editor .unit{color:var(--muted);font-size:12px;white-space:nowrap}
.budget-input,.budget-note{width:100%;border:1px solid #cfd8d2;background:#fff;border-radius:6px;padding:9px 10px;font:inherit;color:var(--ink);outline:none}
.budget-input{min-width:88px;text-align:right}.budget-note{min-width:180px;text-align:left}
.budget-input:focus,.budget-note:focus{border-color:var(--jade);box-shadow:0 0 0 3px rgba(45,107,98,.10)}
.row-total,.group-total{font-variant-numeric:tabular-nums;text-align:right;white-space:nowrap;font-weight:700}.group-total{color:var(--deep)}
.calc-summary{display:grid;grid-template-columns:repeat(4,1fr);gap:1px;background:#d9e1dc;border-top:1px solid #d9e1dc}
.calc-kpi{background:#fff;padding:22px}.calc-kpi span{display:block;color:var(--muted);font-size:11px;letter-spacing:.08em}.calc-kpi b{display:block;margin-top:5px;font-size:23px;color:var(--deep);font-variant-numeric:tabular-nums}
.calc-total{background:var(--deep);color:#fff;padding:26px 28px;display:flex;justify-content:space-between;align-items:center;gap:20px}
.calc-total span{font-size:13px;color:rgba(255,255,255,.68)}.calc-total strong{font-size:34px;color:#e9d09f;white-space:nowrap;font-variant-numeric:tabular-nums}
.calc-foot{padding:15px 28px;background:#f7f9f7;color:var(--muted);font-size:11px;display:flex;justify-content:space-between;gap:18px;flex-wrap:wrap}.saved-tip{color:var(--jade);font-weight:700}
@media(max-width:900px){.calc-head{align-items:flex-start;flex-direction:column}.calc-tools{justify-content:flex-start}.calc-summary{grid-template-columns:1fr 1fr}}
@media(max-width:620px){.calc-summary{grid-template-columns:1fr}.calc-total{align-items:flex-start;flex-direction:column}.calc-total strong{font-size:29px}}
@media print{.calc-tools{display:none!important}.budget-input,.budget-note{border:0;padding:0;background:transparent}.calc-shell{box-shadow:none;break-inside:avoid}}

.budget-reference{
  margin:0 0 28px;padding:24px 28px;background:linear-gradient(135deg,#f8f4eb,#eef4ef);
  border-left:4px solid var(--gold);display:grid;grid-template-columns:auto 1fr;gap:36px;
  align-items:center;color:var(--ink);box-shadow:var(--shadow-soft)
}
.budget-reference-label{display:block;font-size:11px;le